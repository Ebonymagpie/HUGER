function Section({children}) {
    return ( 
        <>
            <section className="flex items-center">
                {children}
            </section>
        </>
     );
}

export default Section;